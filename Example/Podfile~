#use_frameworks!

target 'JSUtility_Example' do
  pod 'JSUtility', :path => '../'

  target 'JSUtility_Tests' do
    inherit! :search_paths

    pod 'Specta'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
  end
end
